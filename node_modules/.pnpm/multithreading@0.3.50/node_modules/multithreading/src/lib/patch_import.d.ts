/**
 * Patches dynamic imports to work in Data URIs / Workers.
 * Universal version: Works in Browser, Deno, and Node.js.
 */
export declare function patchDynamicImports(code: string, callerLocation: string): string;

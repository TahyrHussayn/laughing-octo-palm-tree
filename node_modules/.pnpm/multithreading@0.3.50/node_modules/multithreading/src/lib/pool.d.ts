import type { ThreadTask } from "./types.js";
export declare function workerOverride(fn: () => Worker): void;
export declare class WorkerPool {
    private workers;
    private loadCounts;
    private affinities;
    private pendingResolves;
    private pendingRejects;
    private taskIdMask;
    private maxThreads;
    private taskIdCounter;
    constructor(maxThreads?: number);
    private processArgs;
    private createWorker;
    private nukeWorker;
    submit(task: ThreadTask): Promise<any>;
    terminate(): void;
}
